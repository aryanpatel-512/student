<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>EduNexus — Smart School Management System</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<style>
  /* ─── DESIGN TOKENS ─────────────────────────────────────── */
  :root {
    --ocean-deep:   #1E1E1E;
    --ocean-mid:    #252520;
    --ocean-light:  #2E2E26;
    --accent-cyan:  #A3A375;
    --accent-teal:  #6B6B45;
    --accent-glow:  #D7D8B6;
    --accent-warm:  #C8C89A;
    --gold:         #C4B87A;
    --text-primary: #F5F5F5;
    --text-secondary:#CFCFCF;
    --text-muted:   #8A8A8A;
    --glass-bg:     rgba(255,255,255,0.05);
    --glass-border: rgba(255,255,255,0.08);
    --glass-hover:  rgba(255,255,255,0.08);
    --shadow-glow:  0 0 40px rgba(163,163,117,0.12);
    --shadow-card:  0 8px 32px rgba(0,0,0,0.4);
    --radius-sm:    8px;
    --radius-md:    16px;
    --radius-lg:    24px;
    --radius-xl:    32px;
    --space:        8px;
  }

  /* ─── RESET & BASE ──────────────────────────────────────── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--ocean-deep);
    color: var(--text-primary);
    line-height: 1.6;
    overflow-x: hidden;
  }
  h1,h2,h3,h4,h5 { font-family: 'Syne', sans-serif; line-height: 1.2; }
  a { text-decoration: none; color: inherit; }
  img { max-width: 100%; }

  /* ─── NOISE OVERLAY ─────────────────────────────────────── */
  body::before {
    content: '';
    position: fixed; inset: 0; z-index: 0; pointer-events: none;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    opacity: 0.4;
  }

  /* ─── LAYOUT UTILITIES ──────────────────────────────────── */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 calc(var(--space) * 3);
    position: relative; z-index: 1;
  }
  .section { padding: calc(var(--space) * 12) 0; }
  .section-label {
    font-size: 12px; letter-spacing: 3px; text-transform: uppercase;
    color: var(--accent-cyan); font-weight: 500; margin-bottom: calc(var(--space) * 2);
    display: inline-flex; align-items: center; gap: calc(var(--space));
  }
  .section-label::before {
    content: ''; display: block; width: 24px; height: 1px; background: var(--accent-cyan);
  }
  .section-title {
    font-size: clamp(28px, 4vw, 44px); font-weight: 700; color: var(--text-primary);
    margin-bottom: calc(var(--space) * 2);
  }
  .section-sub { font-size: 17px; color: var(--text-secondary); max-width: 520px; }
  .text-center { text-align: center; }
  .text-center .section-sub { margin: 0 auto; }
  .text-center .section-label { justify-content: center; }
  .gradient-text {
    background: linear-gradient(135deg, var(--accent-cyan), var(--accent-glow));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* ─── GLASS CARD ────────────────────────────────────────── */
  .glass {
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: var(--radius-lg);
    transition: all 0.35s ease;
  }
  .glass:hover {
    background: var(--glass-hover);
    border-color: rgba(163,163,117,0.3);
    box-shadow: var(--shadow-glow), var(--shadow-card);
    transform: translateY(-4px);
  }

  /* ─── BUTTONS ───────────────────────────────────────────── */
  .btn {
    display: inline-flex; align-items: center; gap: calc(var(--space));
    padding: 14px 28px; border-radius: 50px; font-family: 'Syne',sans-serif;
    font-size: 15px; font-weight: 600; cursor: pointer; border: none;
    transition: all 0.3s ease; position: relative; overflow: hidden;
  }
  .btn-primary {
    background: linear-gradient(135deg, var(--accent-teal), #535434);
    color: #F5F5F0;
    box-shadow: 0 4px 20px rgba(107,107,69,0.35);
  }
  .btn-primary::before {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(135deg, var(--accent-cyan), var(--accent-teal));
    opacity: 0; transition: opacity 0.3s;
  }
  .btn-primary:hover::before { opacity: 1; }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 30px rgba(107,107,69,0.45); }
  .btn-secondary {
    background: transparent; color: var(--text-primary);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(10px);
  }
  .btn-secondary:hover {
    border-color: var(--accent-cyan); color: var(--accent-cyan);
    background: rgba(163,163,117,0.08);
    transform: translateY(-2px);
  }
  .btn span { position: relative; z-index: 1; }

  /* ─── NAVBAR ────────────────────────────────────────────── */
  .navbar {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    padding: calc(var(--space) * 2) 0;
    transition: all 0.3s;
  }
  .navbar.scrolled {
    background: rgba(30,30,30,0.9);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--glass-border);
    box-shadow: 0 4px 30px rgba(0,0,0,0.3);
  }
  .nav-inner {
    display: flex; align-items: center; justify-content: space-between;
  }
  .logo {
    font-family: 'Syne', sans-serif; font-size: 22px; font-weight: 800;
    display: flex; align-items: center; gap: calc(var(--space));
  }
  .logo-icon {
    width: 38px; height: 38px; border-radius: 10px;
    background: linear-gradient(135deg, var(--accent-teal), #535434);
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 4px 15px rgba(107,107,69,0.35);
    font-size: 18px;
  }
  .nav-links {
    display: flex; align-items: center; gap: calc(var(--space) * 4);
    list-style: none;
  }
  .nav-links a {
    font-size: 14px; font-weight: 500; color: var(--text-secondary);
    transition: color 0.2s; position: relative;
  }
  .nav-links a::after {
    content: ''; position: absolute; bottom: -4px; left: 0; right: 0;
    height: 1px; background: var(--accent-cyan); transform: scaleX(0);
    transition: transform 0.3s;
  }
  .nav-links a:hover { color: var(--text-primary); }
  .nav-links a:hover::after { transform: scaleX(1); }
  .nav-cta { display: flex; align-items: center; gap: calc(var(--space) * 2); }
  .hamburger { display: none; flex-direction: column; gap: 5px; cursor: pointer; }
  .hamburger span { display: block; width: 24px; height: 2px; background: var(--text-primary); transition: all 0.3s; border-radius: 2px; }

  /* ─── HERO ──────────────────────────────────────────────── */
  .hero {
    min-height: 100vh;
    display: flex; align-items: center;
    padding: calc(var(--space) * 16) 0 calc(var(--space) * 10);
    position: relative; overflow: hidden;
  }
  .hero-bg {
    position: absolute; inset: 0; z-index: 0;
    background: radial-gradient(ellipse 80% 60% at 60% 40%, rgba(46,46,38,0.6) 0%, transparent 70%),
                radial-gradient(ellipse 50% 50% at 10% 80%, rgba(107,107,69,0.1) 0%, transparent 60%),
                linear-gradient(160deg, var(--ocean-deep) 0%, var(--ocean-mid) 50%, var(--ocean-light) 100%);
  }
  .hero-orb {
    position: absolute; border-radius: 50%; filter: blur(80px); pointer-events: none;
  }
  .hero-orb-1 {
    width: 500px; height: 500px; right: -100px; top: -100px;
    background: radial-gradient(circle, rgba(163,163,117,0.12), transparent 70%);
    animation: float 8s ease-in-out infinite;
  }
  .hero-orb-2 {
    width: 300px; height: 300px; left: -50px; bottom: 100px;
    background: radial-gradient(circle, rgba(215,216,182,0.08), transparent 70%);
    animation: float 10s ease-in-out infinite reverse;
  }
  @keyframes float {
    0%,100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-30px) scale(1.05); }
  }
  .hero-grid {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: calc(var(--space) * 8); align-items: center;
    position: relative; z-index: 1;
  }
  .hero-badge {
    display: inline-flex; align-items: center; gap: calc(var(--space));
    padding: 6px 16px; border-radius: 50px;
    background: rgba(163,163,117,0.1); border: 1px solid rgba(163,163,117,0.3);
    font-size: 13px; color: var(--accent-cyan); margin-bottom: calc(var(--space) * 3);
    animation: fadeInDown 0.6s ease;
  }
  .hero-badge-dot {
    width: 6px; height: 6px; background: var(--accent-cyan);
    border-radius: 50%; animation: pulse 2s infinite;
  }
  @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.5;transform:scale(1.4)} }
  .hero-title {
    font-size: clamp(36px, 5vw, 62px); font-weight: 800;
    margin-bottom: calc(var(--space) * 3);
    animation: fadeInUp 0.7s ease 0.1s both;
  }
  .hero-sub {
    font-size: 18px; color: var(--text-secondary); line-height: 1.7;
    margin-bottom: calc(var(--space) * 5);
    animation: fadeInUp 0.7s ease 0.2s both;
    max-width: 480px;
  }
  .hero-btns {
    display: flex; gap: calc(var(--space) * 2); flex-wrap: wrap;
    animation: fadeInUp 0.7s ease 0.3s both;
  }
  .hero-stats {
    display: flex; gap: calc(var(--space) * 5); margin-top: calc(var(--space) * 6);
    animation: fadeInUp 0.7s ease 0.4s both;
  }
  .stat-item h4 { font-size: 28px; font-weight: 800; color: var(--accent-cyan); }
  .stat-item p { font-size: 13px; color: var(--text-secondary); }

  @keyframes fadeInUp { from { opacity:0; transform:translateY(24px); } to { opacity:1; transform:none; } }
  @keyframes fadeInDown { from { opacity:0; transform:translateY(-16px); } to { opacity:1; transform:none; } }

  /* ─── HERO DASHBOARD MOCKUP ──────────────────────────────── */
  .hero-visual {
    position: relative; animation: fadeInRight 0.9s ease 0.3s both;
  }
  @keyframes fadeInRight { from { opacity:0; transform:translateX(40px); } to { opacity:1; transform:none; } }
  .dashboard-card {
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.10);
    backdrop-filter: blur(30px);
    border-radius: var(--radius-xl);
    padding: calc(var(--space) * 3);
    box-shadow: 0 30px 80px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.08);
    transform: perspective(1000px) rotateY(-8deg) rotateX(4deg);
    transition: transform 0.5s ease;
  }
  .dashboard-card:hover { transform: perspective(1000px) rotateY(-2deg) rotateX(1deg); }
  .db-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: calc(var(--space) * 3); padding-bottom: calc(var(--space) * 2);
    border-bottom: 1px solid var(--glass-border);
  }
  .db-title { font-family: 'Syne',sans-serif; font-size: 14px; font-weight: 700; }
  .db-dots { display: flex; gap: 6px; }
  .db-dot { width: 10px; height: 10px; border-radius: 50%; }
  .db-dot:nth-child(1){background:#FF5F56} .db-dot:nth-child(2){background:#FFBD2E} .db-dot:nth-child(3){background:#27C93F}
  .db-stats { display: grid; grid-template-columns: repeat(3,1fr); gap: calc(var(--space) * 2); margin-bottom: calc(var(--space) * 3); }
  .db-stat {
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.07);
    border-radius: var(--radius-md);
    padding: calc(var(--space) * 2);
    text-align: center;
  }
  .db-stat-value { font-family:'Syne',sans-serif; font-size: 22px; font-weight: 800; }
  .db-stat-label { font-size: 11px; color: var(--text-secondary); margin-top: 2px; }
  .db-chart { background: rgba(255,255,255,0.04); border-radius: var(--radius-md); padding: calc(var(--space) * 2); margin-bottom: calc(var(--space) * 2); }
  .db-chart-title { font-size: 11px; color: var(--text-secondary); margin-bottom: calc(var(--space) * 2); }
  .chart-bars { display: flex; align-items: flex-end; gap: 8px; height: 70px; }
  .chart-bar {
    flex: 1; border-radius: 4px 4px 0 0;
    background: linear-gradient(to top, var(--accent-teal), var(--accent-glow));
    opacity: 0.7; transition: opacity 0.2s;
    animation: barRise 1s ease var(--delay, 0s) both;
  }
  .chart-bar:hover { opacity: 1; }
  @keyframes barRise { from{height:0%} to{height:var(--h)} }
  .db-table { display: flex; flex-direction: column; gap: 8px; }
  .db-row {
    display: flex; align-items: center; justify-content: space-between;
    padding: 8px 10px; background: rgba(255,255,255,0.04);
    border-radius: calc(var(--radius-sm)); font-size: 12px;
  }
  .db-row-name { display: flex; align-items: center; gap: 8px; }
  .db-avatar { width: 22px; height: 22px; border-radius: 50%; background: linear-gradient(135deg, var(--accent-teal), var(--accent-warm)); }
  .db-badge-green { padding: 2px 8px; background: rgba(34,197,94,0.15); color: #6DBF82; border-radius: 50px; font-size: 11px; }
  .db-badge-yellow { padding: 2px 8px; background: rgba(196,184,122,0.15); color: var(--gold); border-radius: 50px; font-size: 11px; }

  /* floating accent card */
  .float-accent {
    position: absolute;
    background: rgba(255,255,255,0.07); border: 1px solid rgba(255,255,255,0.10);
    backdrop-filter: blur(20px); border-radius: var(--radius-md);
    padding: calc(var(--space) * 2) calc(var(--space) * 3);
    box-shadow: var(--shadow-card);
    font-size: 13px;
  }
  .float-accent-1 { bottom: -24px; left: -40px; display: flex; align-items: center; gap: 10px; animation: float 6s ease-in-out infinite; }
  .float-accent-2 { top: -20px; right: -30px; display: flex; align-items: center; gap: 10px; animation: float 7s ease-in-out infinite 1s; }
  .fa-icon { font-size: 22px; }

  /* ─── FEATURES ──────────────────────────────────────────── */
  .features-grid {
    display: grid; grid-template-columns: repeat(4,1fr);
    gap: calc(var(--space) * 3); margin-top: calc(var(--space) * 7);
  }
  .feature-card {
    padding: calc(var(--space) * 4);
    border-radius: var(--radius-lg);
    position: relative; overflow: hidden;
  }
  .feature-card::before {
    content: ''; position: absolute; inset: 0; border-radius: inherit;
    background: linear-gradient(135deg, rgba(163,163,117,0.08), transparent 60%);
    opacity: 0; transition: opacity 0.3s;
  }
  .feature-card:hover::before { opacity: 1; }
  .feature-icon {
    width: 54px; height: 54px; border-radius: var(--radius-md);
    display: flex; align-items: center; justify-content: center;
    font-size: 24px; margin-bottom: calc(var(--space) * 3);
    position: relative; z-index: 1;
  }
  .fi-blue { background: rgba(107,107,69,0.18); box-shadow: 0 0 20px rgba(107,107,69,0.18); }
  .fi-teal { background: rgba(163,163,117,0.15); box-shadow: 0 0 20px rgba(163,163,117,0.15); }
  .fi-purple { background: rgba(196,184,122,0.15); box-shadow: 0 0 20px rgba(196,184,122,0.15); }
  .fi-amber { background: rgba(215,216,182,0.12); box-shadow: 0 0 20px rgba(215,216,182,0.12); }
  .feature-card h3 { font-size: 17px; font-weight: 700; margin-bottom: calc(var(--space)); position: relative; z-index: 1; }
  .feature-card p { font-size: 14px; color: var(--text-secondary); line-height: 1.6; position: relative; z-index: 1; }

  /* ─── DASHBOARD PREVIEW ─────────────────────────────────── */
  .preview-bg {
    background: radial-gradient(ellipse 70% 80% at 50% 50%, rgba(46,46,38,0.4) 0%, transparent 70%);
  }
  .preview-grid {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: calc(var(--space) * 4); margin-top: calc(var(--space) * 7);
  }
  .preview-card {
    border-radius: var(--radius-xl);
    padding: calc(var(--space) * 3);
    position: relative; overflow: hidden;
    border: 1px solid transparent;
    background-clip: padding-box;
    transition: all 0.4s ease;
  }
  .preview-card::after {
    content: ''; position: absolute; inset: -1px; border-radius: inherit; z-index: -1;
    background: linear-gradient(135deg, rgba(163,163,117,0.35), rgba(215,216,182,0.18), transparent);
    opacity: 0; transition: opacity 0.4s;
  }
  .preview-card:hover::after { opacity: 1; }
  .preview-card:hover { transform: translateY(-6px); box-shadow: 0 20px 60px rgba(0,0,0,0.4), var(--shadow-glow); }
  .preview-card-label {
    font-size: 11px; letter-spacing: 2px; text-transform: uppercase;
    color: var(--accent-cyan); margin-bottom: calc(var(--space) * 2);
  }
  .preview-card h3 { font-size: 20px; font-weight: 700; margin-bottom: calc(var(--space) * 3); }
  .mini-dashboard {
    background: rgba(0,0,0,0.25); border-radius: var(--radius-md);
    padding: calc(var(--space) * 2); margin-bottom: calc(var(--space) * 2);
  }
  .mini-row { display: flex; gap: calc(var(--space) * 2); margin-bottom: calc(var(--space) * 2); }
  .mini-card {
    flex: 1; background: rgba(255,255,255,0.06);
    border-radius: var(--radius-sm); padding: calc(var(--space) * 2);
    font-size: 12px; border: 1px solid rgba(255,255,255,0.07);
  }
  .mini-card .val { font-family:'Syne',sans-serif; font-size: 20px; font-weight: 700; }
  .mini-card .lbl { font-size: 10px; color: var(--text-muted); margin-top: 2px; }
  .mini-progress { margin-bottom: calc(var(--space) * 1.5); }
  .mini-progress-label { display: flex; justify-content: space-between; font-size: 11px; color: var(--text-secondary); margin-bottom: 4px; }
  .progress-bar { height: 5px; background: rgba(255,255,255,0.08); border-radius: 50px; overflow: hidden; }
  .progress-fill { height: 100%; border-radius: 50px; background: linear-gradient(90deg, var(--accent-teal), var(--accent-glow)); }
  .preview-tags { display: flex; gap: calc(var(--space)); flex-wrap: wrap; }
  .tag {
    padding: 4px 12px; border-radius: 50px; font-size: 12px;
    background: rgba(163,163,117,0.1); border: 1px solid rgba(163,163,117,0.2);
    color: var(--accent-cyan);
  }

  /* ─── ABOUT ─────────────────────────────────────────────── */
  .about-grid {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: calc(var(--space) * 10); align-items: center;
    margin-top: calc(var(--space) * 7);
  }
  .benefit-list { list-style: none; margin-top: calc(var(--space) * 4); display: flex; flex-direction: column; gap: calc(var(--space) * 2); }
  .benefit-item { display: flex; align-items: flex-start; gap: calc(var(--space) * 2); }
  .benefit-icon {
    width: 36px; height: 36px; border-radius: calc(var(--radius-sm));
    background: rgba(163,163,117,0.1); display: flex; align-items: center; justify-content: center;
    font-size: 16px; flex-shrink: 0; margin-top: 2px;
    border: 1px solid rgba(163,163,117,0.2);
  }
  .benefit-item h4 { font-size: 15px; font-weight: 600; margin-bottom: 2px; }
  .benefit-item p { font-size: 13px; color: var(--text-secondary); }
  .about-visual {
    position: relative;
  }
  .about-big-card {
    border-radius: var(--radius-xl);
    padding: calc(var(--space) * 4);
    background: linear-gradient(135deg, rgba(255,255,255,0.07), rgba(255,255,255,0.02));
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(20px);
  }
  .about-img-placeholder {
    width: 100%; height: 280px;
    background: linear-gradient(135deg, rgba(107,107,69,0.12), rgba(46,46,38,0.4));
    border-radius: var(--radius-md); margin-bottom: calc(var(--space) * 3);
    display: flex; align-items: center; justify-content: center;
    font-size: 60px; position: relative; overflow: hidden;
  }
  .about-img-placeholder::before {
    content: ''; position: absolute; inset: 0;
    background: repeating-linear-gradient(
      45deg, rgba(163,163,117,0.03) 0px, rgba(163,163,117,0.03) 1px, transparent 1px, transparent 40px
    );
  }

  /* ─── ADMISSION ─────────────────────────────────────────── */
  .admission-section {
    position: relative; overflow: hidden;
  }
  .admission-inner {
    background: linear-gradient(135deg, var(--ocean-mid), rgba(46,46,38,0.7));
    border: 1px solid rgba(163,163,117,0.2);
    border-radius: var(--radius-xl);
    padding: calc(var(--space) * 10) calc(var(--space) * 8);
    display: flex; align-items: center; justify-content: space-between;
    gap: calc(var(--space) * 5); flex-wrap: wrap;
    position: relative; overflow: hidden;
    box-shadow: 0 0 80px rgba(163,163,117,0.06), var(--shadow-card);
  }
  .admission-inner::before {
    content: ''; position: absolute;
    width: 400px; height: 400px; right: -100px; top: -100px;
    background: radial-gradient(circle, rgba(163,163,117,0.08), transparent 70%);
    border-radius: 50%;
  }
  .admission-steps {
    display: flex; gap: calc(var(--space) * 3); flex-wrap: wrap;
    margin-top: calc(var(--space) * 3);
  }
  .step {
    display: flex; align-items: center; gap: calc(var(--space) * 2);
    font-size: 14px; color: var(--text-secondary);
  }
  .step-num {
    width: 32px; height: 32px; border-radius: 50%;
    background: linear-gradient(135deg, var(--accent-teal), #535434);
    display: flex; align-items: center; justify-content: center;
    font-family: 'Syne',sans-serif; font-weight: 700; font-size: 13px;
    flex-shrink: 0;
  }

  /* ─── TESTIMONIALS ──────────────────────────────────────── */
  .testimonials-grid {
    display: grid; grid-template-columns: repeat(3,1fr);
    gap: calc(var(--space) * 3); margin-top: calc(var(--space) * 7);
  }
  .testimonial-card { padding: calc(var(--space) * 4); position: relative; }
  .testimonial-card::before {
    content: '"'; position: absolute; top: 16px; right: 24px;
    font-size: 80px; font-family: 'Syne',sans-serif; font-weight: 800;
    color: rgba(163,163,117,0.1); line-height: 1;
  }
  .stars { display: flex; gap: 3px; margin-bottom: calc(var(--space) * 2); }
  .star { color: var(--gold); font-size: 14px; }
  .testimonial-text { font-size: 15px; color: var(--text-secondary); line-height: 1.7; margin-bottom: calc(var(--space) * 3); }
  .testimonial-author { display: flex; align-items: center; gap: calc(var(--space) * 2); }
  .author-avatar {
    width: 44px; height: 44px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 18px; font-weight: 700; font-family: 'Syne',sans-serif;
    flex-shrink: 0;
  }
  .av1 { background: linear-gradient(135deg, #6B6B45, #535434); }
  .av2 { background: linear-gradient(135deg, #7A7A52, #4E4E38); }
  .av3 { background: linear-gradient(135deg, #8A8A62, #535434); }
  .author-name { font-size: 14px; font-weight: 600; }
  .author-role { font-size: 12px; color: var(--text-muted); }

  /* ─── FOOTER ────────────────────────────────────────────── */
  .footer {
    background: rgba(0,0,0,0.3);
    border-top: 1px solid var(--glass-border);
    padding: calc(var(--space) * 10) 0 calc(var(--space) * 5);
  }
  .footer-grid {
    display: grid; grid-template-columns: 2fr 1fr 1fr 1.5fr;
    gap: calc(var(--space) * 6); margin-bottom: calc(var(--space) * 8);
  }
  .footer-brand p { font-size: 14px; color: var(--text-secondary); margin-top: calc(var(--space) * 2); line-height: 1.7; max-width: 260px; }
  .footer-col h5 { font-size: 14px; font-weight: 700; margin-bottom: calc(var(--space) * 3); color: var(--text-primary); }
  .footer-links { list-style: none; display: flex; flex-direction: column; gap: calc(var(--space) * 1.5); }
  .footer-links a { font-size: 14px; color: var(--text-secondary); transition: color 0.2s; }
  .footer-links a:hover { color: var(--accent-cyan); }
  .footer-contact p { font-size: 14px; color: var(--text-secondary); display: flex; gap: 8px; align-items: flex-start; margin-bottom: calc(var(--space) * 2); }
  .social-links { display: flex; gap: calc(var(--space) * 2); margin-top: calc(var(--space) * 3); }
  .social-link {
    width: 38px; height: 38px; border-radius: var(--radius-sm);
    background: var(--glass-bg); border: 1px solid var(--glass-border);
    display: flex; align-items: center; justify-content: center;
    font-size: 16px; transition: all 0.3s; cursor: pointer;
  }
  .social-link:hover { border-color: var(--accent-cyan); background: rgba(163,163,117,0.1); transform: translateY(-2px); }
  .footer-bottom {
    padding-top: calc(var(--space) * 4);
    border-top: 1px solid var(--glass-border);
    display: flex; align-items: center; justify-content: space-between;
    font-size: 13px; color: var(--text-muted); flex-wrap: wrap; gap: calc(var(--space) * 2);
  }

  /* ─── RESPONSIVE ────────────────────────────────────────── */
  @media (max-width: 1024px) {
    .features-grid { grid-template-columns: repeat(2,1fr); }
    .footer-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 768px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero-visual { display: none; }
    .preview-grid { grid-template-columns: 1fr; }
    .about-grid { grid-template-columns: 1fr; }
    .testimonials-grid { grid-template-columns: 1fr; }
    .nav-links, .nav-cta { display: none; }
    .hamburger { display: flex; }
    .features-grid { grid-template-columns: 1fr; }
    .admission-inner { flex-direction: column; text-align: center; padding: calc(var(--space)*6) calc(var(--space)*4); }
    .footer-grid { grid-template-columns: 1fr; }
    .footer-bottom { flex-direction: column; text-align: center; }
    .hero-stats { gap: calc(var(--space)*3); }
  }

  /* ─── SCROLL REVEAL ─────────────────────────────────────── */
  .reveal {
    opacity: 0; transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.visible { opacity: 1; transform: none; }
  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }
  .reveal-delay-4 { transition-delay: 0.4s; }

  /* ─── DIVIDER ────────────────────────────────────────────── */
  .divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--glass-border), transparent);
    margin: 0;
  }
</style>
</head>
<body>

<!-- ═══ NAVBAR ═══════════════════════════════════════════════════ -->
<nav class="navbar" id="navbar">
  <div class="container">
    <div class="nav-inner">
      <a href="#" class="logo">
        <div class="logo-icon">🎓</div>
        <span>Edu<span class="gradient-text">Nexus</span></span>
      </a>
      <ul class="nav-links">
        <li><a href="#features">Features</a></li>
        <li><a href="#dashboard">Dashboard</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="admission-requirements.html">Admission</a></li>
        <li><a href="#testimonials">Reviews</a></li>
      </ul>
      <div class="nav-cta">
        <a href="#" class="btn btn-secondary"><span>Sign In</span></a>
        <a href="#" class="btn btn-primary"><span>Get Started</span></a>
      </div>
      <div class="hamburger" onclick="this.classList.toggle('open')">
        <span></span><span></span><span></span>
      </div>
    </div>
  </div>
</nav>

<!-- ═══ HERO ══════════════════════════════════════════════════════ -->
<section class="hero" id="home">
  <div class="hero-bg"></div>
  <div class="hero-orb hero-orb-1"></div>
  <div class="hero-orb hero-orb-2"></div>
  <div class="container">
    <div class="hero-grid">
      <div class="hero-content">
        <div class="hero-badge">
          <span class="hero-badge-dot"></span>
          Now with AI-powered Insights ✨
        </div>
        <h1 class="hero-title">
          Smart School<br>
          <span class="gradient-text">Management</span><br>
          System
        </h1>
        <p class="hero-sub">
          Streamline every aspect of school operations — from student enrollment to performance analytics — in one elegant, unified platform.
        </p>
        <div class="hero-btns">
          <a href="#" class="btn btn-primary"><span>🚀 Get Started Free</span></a>
          <a href="#features" class="btn btn-secondary"><span>Explore Features →</span></a>
        </div>
        <div class="hero-stats">
          <div class="stat-item">
            <h4>500+</h4>
            <p>Schools Enrolled</p>
          </div>
          <div class="stat-item">
            <h4>98%</h4>
            <p>Satisfaction Rate</p>
          </div>
          <div class="stat-item">
            <h4>2M+</h4>
            <p>Students Managed</p>
          </div>
        </div>
      </div>

      <!-- Dashboard Mockup -->
      <div class="hero-visual">
        <div class="dashboard-card">
          <div class="db-header">
            <div class="db-dots">
              <div class="db-dot"></div><div class="db-dot"></div><div class="db-dot"></div>
            </div>
            <div class="db-title">School Dashboard — 2024–25</div>
            <div style="font-size:11px;color:var(--text-muted)">Live</div>
          </div>

          <div class="db-stats">
            <div class="db-stat">
              <div class="db-stat-value" style="color:var(--accent-cyan)">1,248</div>
              <div class="db-stat-label">Students</div>
            </div>
            <div class="db-stat">
              <div class="db-stat-value" style="color:#6DBF82">94%</div>
              <div class="db-stat-label">Attendance</div>
            </div>
            <div class="db-stat">
              <div class="db-stat-value" style="color:var(--accent-warm)">₹8.2L</div>
              <div class="db-stat-label">Fees Collected</div>
            </div>
          </div>

          <div class="db-chart">
            <div class="db-chart-title">Monthly Attendance Overview</div>
            <div class="chart-bars">
              <div class="chart-bar" style="--h:65%;--delay:0s;"></div>
              <div class="chart-bar" style="--h:80%;--delay:.05s;"></div>
              <div class="chart-bar" style="--h:55%;--delay:.1s;"></div>
              <div class="chart-bar" style="--h:90%;--delay:.15s;"></div>
              <div class="chart-bar" style="--h:72%;--delay:.2s;"></div>
              <div class="chart-bar" style="--h:85%;--delay:.25s;"></div>
              <div class="chart-bar" style="--h:95%;--delay:.3s;"></div>
              <div class="chart-bar" style="--h:78%;--delay:.35s;"></div>
            </div>
          </div>

          <div class="db-table">
            <div class="db-row">
              <div class="db-row-name">
                <div class="db-avatar" style="background:linear-gradient(135deg,#6B6B45,#535434)"></div>
                <span>Arjun Mehta — Grade 10A</span>
              </div>
              <span class="db-badge-green">Present</span>
            </div>
            <div class="db-row">
              <div class="db-row-name">
                <div class="db-avatar" style="background:linear-gradient(135deg,#C4B87A,#A3A375)"></div>
                <span>Priya Sharma — Grade 9B</span>
              </div>
              <span class="db-badge-yellow">Late</span>
            </div>
            <div class="db-row">
              <div class="db-row-name">
                <div class="db-avatar" style="background:linear-gradient(135deg,#8A8A62,#6B6B45)"></div>
                <span>Rohan Patel — Grade 11C</span>
              </div>
              <span class="db-badge-green">Present</span>
            </div>
          </div>
        </div>

        <!-- Floating accent cards -->
        <div class="float-accent float-accent-1">
          <span class="fa-icon">📈</span>
          <div>
            <div style="font-weight:700;font-size:15px;color:#6DBF82">+12.5%</div>
            <div style="font-size:11px;color:var(--text-muted)">Performance this term</div>
          </div>
        </div>
        <div class="float-accent float-accent-2">
          <span class="fa-icon">🔔</span>
          <div>
            <div style="font-weight:600;font-size:13px;">3 New Admissions</div>
            <div style="font-size:11px;color:var(--text-muted)">Pending approval</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ═══ FEATURES ══════════════════════════════════════════════════ -->
<section class="section" id="features">
  <div class="container">
    <div class="text-center">
      <div class="section-label reveal">Core Features</div>
      <h2 class="section-title reveal">Everything You Need to<br><span class="gradient-text">Run Your School</span></h2>
      <p class="section-sub reveal">Purpose-built tools that cover every workflow, from enrollment to graduation, designed for educators and administrators alike.</p>
    </div>
    <div class="features-grid">
      <div class="glass feature-card reveal reveal-delay-1">
        <div class="feature-icon fi-blue">👨‍🎓</div>
        <h3>Student Management</h3>
        <p>Complete student profiles with academic records, attendance history, health info, and parent contacts — all in one place.</p>
      </div>
      <div class="glass feature-card reveal reveal-delay-2">
        <div class="feature-icon fi-teal">📋</div>
        <h3>Attendance Tracking</h3>
        <p>Real-time biometric and digital attendance with instant SMS/email alerts to parents and automated monthly reports.</p>
      </div>
      <div class="glass feature-card reveal reveal-delay-3">
        <div class="feature-icon fi-purple">💳</div>
        <h3>Fee Management</h3>
        <p>Streamlined online fee collection, automated reminders, receipt generation, and outstanding dues tracking.</p>
      </div>
      <div class="glass feature-card reveal reveal-delay-4">
        <div class="feature-icon fi-amber">📊</div>
        <h3>Reports & Analytics</h3>
        <p>Insightful dashboards with performance trends, attendance patterns, fee analytics, and exportable PDF/Excel reports.</p>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ═══ DASHBOARD PREVIEW ══════════════════════════════════════════ -->
<section class="section preview-bg" id="dashboard">
  <div class="container">
    <div class="text-center">
      <div class="section-label reveal">Live Previews</div>
      <h2 class="section-title reveal">Powerful <span class="gradient-text">Dashboard Overview</span></h2>
      <p class="section-sub reveal">Role-based dashboards give every stakeholder exactly the insights they need — nothing more, nothing less.</p>
    </div>
    <div class="preview-grid">
      <!-- Student Dashboard -->
      <div class="glass preview-card reveal">
        <div class="preview-card-label">Student Portal</div>
        <h3>Student Dashboard</h3>
        <div class="mini-dashboard">
          <div class="mini-row">
            <div class="mini-card">
              <div class="val" style="color:var(--accent-cyan)">87%</div>
              <div class="lbl">Overall Score</div>
            </div>
            <div class="mini-card">
              <div class="val" style="color:#6DBF82">92%</div>
              <div class="lbl">Attendance</div>
            </div>
            <div class="mini-card">
              <div class="val" style="color:var(--gold)">A+</div>
              <div class="lbl">Last Exam</div>
            </div>
          </div>
          <div class="mini-progress">
            <div class="mini-progress-label"><span>Mathematics</span><span>91%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:91%"></div></div>
          </div>
          <div class="mini-progress">
            <div class="mini-progress-label"><span>Science</span><span>84%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:84%;background:linear-gradient(90deg,#8A8A62,#D7D8B6)"></div></div>
          </div>
          <div class="mini-progress">
            <div class="mini-progress-label"><span>English</span><span>78%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:78%;background:linear-gradient(90deg,#C4B87A,#D7D8B6)"></div></div>
          </div>
        </div>
        <div class="preview-tags">
          <span class="tag">📚 Assignments</span>
          <span class="tag">📅 Timetable</span>
          <span class="tag">🎯 Goals</span>
        </div>
      </div>

      <!-- Principal Dashboard -->
      <div class="glass preview-card reveal reveal-delay-2">
        <div class="preview-card-label">Admin Portal</div>
        <h3>Principal Dashboard</h3>
        <div class="mini-dashboard">
          <div class="mini-row">
            <div class="mini-card">
              <div class="val" style="color:var(--accent-cyan)">1,248</div>
              <div class="lbl">Total Students</div>
            </div>
            <div class="mini-card">
              <div class="val" style="color:#6DBF82">86</div>
              <div class="lbl">Staff Members</div>
            </div>
            <div class="mini-card">
              <div class="val" style="color:var(--accent-warm)">32</div>
              <div class="lbl">Classes</div>
            </div>
          </div>
          <div class="mini-progress">
            <div class="mini-progress-label"><span>Fee Collection</span><span>78%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:78%"></div></div>
          </div>
          <div class="mini-progress">
            <div class="mini-progress-label"><span>Staff Attendance</span><span>96%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:96%;background:linear-gradient(90deg,#6DBF82,#9ADBA8)"></div></div>
          </div>
          <div class="mini-progress">
            <div class="mini-progress-label"><span>Admission Progress</span><span>65%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:65%;background:linear-gradient(90deg,#8A8A62,#D7D8B6)"></div></div>
          </div>
        </div>
        <div class="preview-tags">
          <span class="tag">📊 Analytics</span>
          <span class="tag">👥 Staff</span>
          <span class="tag">💰 Finance</span>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ═══ ABOUT ══════════════════════════════════════════════════════ -->
<section class="section" id="about">
  <div class="container">
    <div class="about-grid">
      <div class="reveal">
        <div class="section-label">Why EduNexus</div>
        <h2 class="section-title">Built for Schools,<br><span class="gradient-text">Loved by Everyone</span></h2>
        <p class="section-sub">We designed EduNexus from the ground up in partnership with school administrators, teachers, and parents to solve real, everyday challenges.</p>
        <ul class="benefit-list">
          <li class="benefit-item">
            <div class="benefit-icon">⚡</div>
            <div>
              <h4>Incredibly Easy to Use</h4>
              <p>Intuitive interface requires zero technical expertise. Get up and running in minutes, not months.</p>
            </div>
          </li>
          <li class="benefit-item">
            <div class="benefit-icon">⏱️</div>
            <div>
              <h4>Save 10+ Hours Weekly</h4>
              <p>Automate repetitive tasks like attendance, fee reminders, and report generation.</p>
            </div>
          </li>
          <li class="benefit-item">
            <div class="benefit-icon">🔒</div>
            <div>
              <h4>Bank-Grade Security</h4>
              <p>AES-256 encryption, role-based access control, and daily automated backups keep your data safe.</p>
            </div>
          </li>
          <li class="benefit-item">
            <div class="benefit-icon">📱</div>
            <div>
              <h4>Works on Any Device</h4>
              <p>Fully responsive web app plus dedicated Android and iOS apps for on-the-go access.</p>
            </div>
          </li>
        </ul>
      </div>
      <div class="about-visual reveal reveal-delay-2">
        <div class="about-big-card">
          <div class="about-img-placeholder">🏫</div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
            <div class="glass" style="padding:16px;border-radius:var(--radius-md);text-align:center">
              <div style="font-size:28px;font-family:'Syne',sans-serif;font-weight:800;color:var(--accent-cyan)">500+</div>
              <div style="font-size:12px;color:var(--text-secondary)">Happy Schools</div>
            </div>
            <div class="glass" style="padding:16px;border-radius:var(--radius-md);text-align:center">
              <div style="font-size:28px;font-family:'Syne',sans-serif;font-weight:800;color:#6DBF82">4.9★</div>
              <div style="font-size:12px;color:var(--text-secondary)">Average Rating</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ═══ ADMISSION ══════════════════════════════════════════════════ -->
<section class="section admission-section" id="admission">
  <div class="container">
    <div class="admission-inner reveal">
      <div>
        <div class="section-label" style="margin-bottom:12px">Admissions 2024–25</div>
        <h2 class="section-title" style="margin-bottom:12px">Seamless <span class="gradient-text">Admission Process</span></h2>
        <p style="color:var(--text-secondary);font-size:15px;max-width:480px">From online application to document verification and fee payment — manage the entire admission lifecycle digitally, without a single paper form.</p>
        <div class="admission-steps">
          <div class="step"><div class="step-num">1</div><span>Online Application</span></div>
          <div class="step"><div class="step-num">2</div><span>Document Upload</span></div>
          <div class="step"><div class="step-num">3</div><span>Review & Approve</span></div>
          <div class="step"><div class="step-num">4</div><span>Fee Payment</span></div>
        </div>
      </div>
      <div style="flex-shrink:0">
        <a href="#" class="btn btn-primary" style="font-size:16px;padding:16px 36px;white-space:nowrap">
          <span>📋 View Admission Process</span>
        </a>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ═══ TESTIMONIALS ═══════════════════════════════════════════════ -->
<section class="section" id="testimonials">
  <div class="container">
    <div class="text-center">
      <div class="section-label reveal">Testimonials</div>
      <h2 class="section-title reveal">Trusted by <span class="gradient-text">School Leaders</span></h2>
      <p class="section-sub reveal">Hear from the educators and administrators who transformed their schools with EduNexus.</p>
    </div>
    <div class="testimonials-grid">
      <div class="glass testimonial-card reveal reveal-delay-1">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"EduNexus has completely transformed how we manage our school. Attendance, fees, and reports are now a breeze. Our teachers save hours every week!"</p>
        <div class="testimonial-author">
          <div class="author-avatar av1">AK</div>
          <div>
            <div class="author-name">Amit Kumar</div>
            <div class="author-role">Principal, Delhi Public School</div>
          </div>
        </div>
      </div>
      <div class="glass testimonial-card reveal reveal-delay-2">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"The parent communication tools are outstanding. We can now reach every parent instantly, and they love having visibility into their child's progress."</p>
        <div class="testimonial-author">
          <div class="author-avatar av2">SR</div>
          <div>
            <div class="author-name">Sunita Rao</div>
            <div class="author-role">Vice Principal, St. Mary's Academy</div>
          </div>
        </div>
      </div>
      <div class="glass testimonial-card reveal reveal-delay-3">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"We moved from paper-based fee collection to EduNexus and reduced defaults by 40%. The automated reminders alone have paid for the entire subscription."</p>
        <div class="testimonial-author">
          <div class="author-avatar av3">MP</div>
          <div>
            <div class="author-name">Manish Patel</div>
            <div class="author-role">Administrator, Greenwood International</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ═══ FOOTER ══════════════════════════════════════════════════════ -->
<footer class="footer">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <a href="#" class="logo" style="margin-bottom:16px;display:inline-flex">
          <div class="logo-icon">🎓</div>
          <span>Edu<span class="gradient-text">Nexus</span></span>
        </a>
        <p>The all-in-one school management platform built for modern educational institutions across India and beyond.</p>
        <div class="social-links">
          <div class="social-link">𝕏</div>
          <div class="social-link">in</div>
          <div class="social-link">f</div>
          <div class="social-link">▶</div>
        </div>
      </div>
      <div class="footer-col">
        <h5>Product</h5>
        <ul class="footer-links">
          <li><a href="#">Features</a></li>
          <li><a href="#">Dashboard</a></li>
          <li><a href="#">Pricing</a></li>
          <li><a href="#">Changelog</a></li>
          <li><a href="#">Roadmap</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h5>Support</h5>
        <ul class="footer-links">
          <li><a href="#">Help Center</a></li>
          <li><a href="#">Documentation</a></li>
          <li><a href="#">Video Tutorials</a></li>
          <li><a href="#">Community</a></li>
          <li><a href="#">Contact Us</a></li>
        </ul>
      </div>
      <div class="footer-col footer-contact">
        <h5>Contact</h5>
        <p>📧 hello@edunexus.in</p>
        <p>📞 +91 98765 43210</p>
        <p>📍 Ahmedabad, Gujarat, India</p>
        <a href="#" class="btn btn-primary" style="margin-top:12px;padding:10px 20px;font-size:13px"><span>🚀 Book a Demo</span></a>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© 2024 EduNexus Technologies Pvt. Ltd. All rights reserved.</span>
      <div style="display:flex;gap:24px;flex-wrap:wrap">
        <a href="#" style="color:var(--text-muted);transition:color 0.2s" onmouseover="this.style.color='var(--accent-cyan)'" onmouseout="this.style.color='var(--text-muted)'">Privacy Policy</a>
        <a href="#" style="color:var(--text-muted);transition:color 0.2s" onmouseover="this.style.color='var(--accent-cyan)'" onmouseout="this.style.color='var(--text-muted)'">Terms of Service</a>
        <a href="#" style="color:var(--text-muted);transition:color 0.2s" onmouseover="this.style.color='var(--accent-cyan)'" onmouseout="this.style.color='var(--text-muted)'">Cookie Policy</a>
      </div>
    </div>
  </div>
</footer>

<script>
  // Navbar scroll
  const navbar = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    navbar.classList.toggle('scrolled', window.scrollY > 40);
  });

  // Scroll reveal
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); } });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // Chart bar heights (CSS custom prop approach)
  const bars = document.querySelectorAll('.chart-bar');
  const heights = [65,80,55,90,72,85,95,78];
  bars.forEach((b,i) => b.style.height = heights[i] + '%');
</script>
</body>
</html>
